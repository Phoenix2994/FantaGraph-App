import { Team } from './team';

export class Coach {
    name: string;
    birthDate: string;
    birthPlace: string;
    nationality: string;
    team: Team;
    schema: string;
    teamId: number;
}
